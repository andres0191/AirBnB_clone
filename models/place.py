#!/usr/bin/python3
from models.base_model import BaseModel


class Place(BaseModel)
    def __init__(self, *args, **kwargs):
        if args = 'City.id':
            city_id = str(uuid.uuid4())
        if args = 'User.id':
            user_id = str(uuid.uuid4())
        if args = 'name':
            name = str('')
        if args = 'description':
            description = str('')
        if args = 'number_rooms':
            number_rooms = int(0)
        if args = 'number_bathrooms':
            number_bathrooms = int(0)
        if args = 'max_guest':
            max_guest = int(0)
        if args = 'price_by_night'
            price_by_night = int(0)
        if args = 'latitude'
            latitude = float(0.0)
        if args = 'longitude'
            longitude = float(0.0)
        if args = 'amenity_ids'
            amenity_ids = list()